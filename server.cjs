const http=require('node:http'),fs=require('node:fs'),path=require('node:path');const files={'/':'index.html','/index.html':'index.html','/style.css':'style.css','/game.js':'game.js'};http.createServer((req,res)=>{const file=files[new URL(req.url,'http://localhost').pathname];if(!file){res.writeHead(404);return res.end('Not found');}res.setHeader('Content-Type',{'html':'text/html; charset=utf-8','css':'text/css','js':'text/javascript'}[file.split('.').pop()]);fs.createReadStream(path.join(__dirname,file)).pipe(res);}).listen(process.env.PORT||3000,'0.0.0.0',()=>console.log('Turbo Trails is ready on port '+(process.env.PORT||3000)));

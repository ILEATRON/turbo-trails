const fs=require('node:fs');fs.mkdirSync('dist',{recursive:true});for(const file of ['index.html','style.css','game.js','sim.js'])fs.copyFileSync(file,'dist/'+file);console.log('Built 3D client in dist/; multiplayer requires node server.cjs');
